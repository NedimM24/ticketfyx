<!DOCTYPE html>
<html lang="en">
<head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>TicketFyx</title>
        <link rel="stylesheet" href="/css/styles.css">
        
</head>
<body>
        <main>
                <%- include("partials/header.ejs") %>

                <!-- if the user is a worker, they can click to become a dev
                will move this into header eventtually -->

                <% if(user.role === 'worker'){%>
                        <a href="/devRegister">Become a dev</a>
                <% } %>

                <a href="/newTicket">Create a new ticket </a>
                
                <h1>hey <%= user.email%></h1>
                <h2>You are a <%= user.role%></h2>
                

                <h1>Tickets</h1>
                <% tickets.forEach(function(ticket) {%>
                        <div class="ticket-container">
                                <a class="ticket-link" href="/ticketPage/<%= ticket.id%>">
                                        <div class="ticket">
                                                <p><b>Title: </b><%= ticket.title%></p>
                                                <p><b>Description: </b><%= ticket.description%></p>
                                                <p><b>Status: </b><%= ticket.status%></p>
                                                <p><b>Priority: </b><%= ticket.priority%></p>
                                                <p><b>Created by: </b><%= ticket.creator_email%></p>
                                                <p><b>Date Created: </b><%= ticket.ticket_creation_date%></p>
                                                <p><b>Assigned Dev: </b><%= ticket.assigned_developer_email ?? "Unassigned"%></p>
                                        </div> 
                                </a>
                        </div>
                <% }) %>
                <%- include("partials/footer.ejs") %>
        </main>
</body>
</html>